angular.module("starter.controllers")

    .controller("ClientCheckoutCtrl", ['$scope', 'OAuth', '$ionicPopup', '$state', function ($scope, OAuth, $ionicPopup, $state) {

        console.log('ClientCheckoutCtrl');
    }
    ]);